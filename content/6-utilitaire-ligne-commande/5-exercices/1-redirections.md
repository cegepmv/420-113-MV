+++
pre = '<b>1. </b>'
title = "Redirections"
weight = "651"
+++
---------------------


1. Quelle commande permet de créer un fichier nommé `f.txt` contenant la phrase "Le soleil brille" sans utiliser un éditeur de texte?

2. Quelle commande permet d’ajouter la ligne "La lune se lève" au fichier `f.txt` sans effacer son contenu?

3. Redirigez la sortie de la commande `ip address` dans un fichier nommé `ip.txt`.
 ip address > ip.txt

4. Le fichier `/etc/machine-id` contient un identifiant unique pour le système linux. En une seule commande et en utilisant la redirection, stockez cette valeur dans une variable nommée `MACHINEID`.

5. Utilisez `head` pour afficher les 2 premières lignes du fichier `/etc/hosts`.

6. Redirigez le résultat de ls vers la commande `head` pour afficher les 5 premiers fichiers du répertoire `/etc`. 

7. Utilisez la redirection pour écrire dans un fichier nommé `liste.txt` tous les fichiers du répertoire `/etc/systemd` ayant l’extension `.conf`

8. Utilisez la redirection pour écrire dans un fichier nommé `liste2.txt` les 3 premiers  fichiers du répertoire `/etc/systemd` ayant l’extension `.conf`

9. Comment utiliser la redirection avec les commandes `head` et `tail` pour voir l’avant-dernière ligne du fichier `/etc/group`?