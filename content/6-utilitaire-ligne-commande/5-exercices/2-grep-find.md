+++
pre = '<b>2. </b>'
title = "grep et find"
weight = "652"
+++
---------------------
1. Quelle commande affiche toutes les lignes du fichier `/etc/passwd` qui contiennent la chaîne de caractères `/bin/bash`?

2. Quelle commande affiche le nombre de lignes du fichier `/etc/passwd` qui contiennent la chaîne de caractères `/bin/bash`?

3. Affichez les noms de tous les fichiers dans `/var/log` qui contiennent la chaîne de caractères "*kernel*"

4. Affichez tous les fichiers ou répertoires dans `/etc` et ses sous-répertoires dont le nom contient "*network*", en majuscules ou en minuscules.

<!-- 5. Affichez tous les fichiers dans le répertoire courant ayant les permissions 644.

6. Affichez tous les fichiers dans l’ensemble du système qui appartiennent à l’utilisateur info et dont la taille est inférieure à 1k. -->

7. Utilisez la redirection pour créer un fichier nommé `kernel.txt` ayant la liste de tous les fichiers dans le système dont le nom contient la chaîne de caractères "*kernel*".

8. Utilisez la redirection pour écrire dans `resultats.txt` les lignes de tous les fichiers dans `/etc/` et ses sous-répertoires qui contiennent le terme "*debian.org*".