+++
pre = '<b>3. </b>'
title = "sort, uniq, wc et cut"
weight = "663"
+++
---------------------

1. Quelle commande permet de trier en ordre alphabétique inversé le contenu de `/etc/passwd`?


2. Utilisez la redirection pour trier en ordre alphabétique inversé les fichiers et les répertoires dans `/etc`.


3. Quelle commande permet d’afficher le nombre de lignes dans le fichier `/etc/group`?


4. Utilisez la redirection entre `ls` et `wc` pour afficher le nombre d’éléments dans le répertoire `/usr/share`.

5. Utilisez la redirection entre `ls`, `grep` et `wc` pour afficher le nombre d’éléments dans le répertoire `/usr/share` qui contiennent le terme "*gnome*"

6. Utilisez la redirection pour envoyer dans un fichier nommé `proc.txt` tous les résultats de la commande `ps -aux` qui contiennent le terme "*gnome*".

7. Utilisez la commande `cut` pour afficher ce qui précède le caractère `/` dans les lignes du fichier `/etc/mime.types`

8. Utilisez la redirection entre `ps -aux` et `cut` pour conserver seulement la valeur de la première colonne des résultats

9. Utilisez la redirection entre `ps -aux`, `cut` et `sort` pour trier la valeur de la première colonne des résultats

10. Utilisez la redirection pour afficher seulement les valeurs distinctes dans les résultats de la question précédente.

